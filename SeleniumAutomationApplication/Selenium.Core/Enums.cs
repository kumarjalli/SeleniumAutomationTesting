namespace Selenium.Core
{
    public enum OperatingSystem
    {
        Windows,
        Mac,
        iOS
    }
    public enum Browsers
    {
        Chrome,
        FireFox,
        InternetExplorer,
        Safari,
        Android
    }
}